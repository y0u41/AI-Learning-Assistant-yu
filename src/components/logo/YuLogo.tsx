/**
 * "yu" 字标（PRD §6.5 / Tech_Design §9）。M2 实现。
 * TODO(M2): 内联 SVG 连笔字标（round cap/join）+ 玻璃容器层；
 * pathLength 描边入场约 1.2s（sessionStorage 'yu-logo-intro' 每会话一次）
 * + 待机呼吸（流光，周期 ≥ 4s，透明度 ±8%）；
 * props: size、animated（false 时全静态，用于 Footer/打印）。
 */
export default function YuLogo() {
  return null;
}