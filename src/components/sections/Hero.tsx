/**
 * 首屏 Hero（PRD F1）：YuLogo + H1「yu 的作品集」+ 简介 + 头像 + 双按钮 + 滚动指示。
 * TODO(M1): 内容读 profile；<h1> 必须是页面唯一 h1；
 * TODO(M2): Logo 描边入场（每会话一次）与滚动指示浮动，均须 prefers-reduced-motion 降级。
 */
export default function Hero() {
  return null;
}