/**
 * 项目展示（PRD F4，核心分区）。M1 静态网格，M2 补筛选与动效。
 * TODO(M1): 读 projects；pinned → date 降序稳定排序；
 * TODO(M2): ProjectFilter（useState，不引状态库）+ 空状态 + ≤200ms 列表过渡。
 */
export default function Projects() {
  return null;
}