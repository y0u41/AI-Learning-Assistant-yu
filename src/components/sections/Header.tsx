/**
 * 吸顶玻璃导航（PRD F2）。M1 静态版（锚点 + 高亮），M2 补玻璃滚动态与移动端抽屉。
 * TODO(M1): useActiveSection 高亮当前分区（当前项 92% 白，其余 65% 白）；
 * TODO(M2): ≤768px 汉堡按钮 + 全宽玻璃抽屉，点击菜单项滚动并收起；
 * TODO(M3): 抽屉 Esc 可关、焦点可见（Build.md §2.4）。
 */
export default function Header() {
  return null;
}