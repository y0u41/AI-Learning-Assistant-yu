/**
 * 项目卡片（PRD F4.1）。M2 实现完整视觉。
 * TODO(M2): 16:10 封面懒加载 + width/height 占位防抖版（无封面渲染类型专属玻璃占位图形）、
 * 类型徽标胶囊（等宽字体）、名称 + 置顶 ☆、简介（≤60 字）、技术栈 chips（≤4 + N）、
 * ProjectLinks 链接组；整卡点击 → links[0].url（内部按钮 stopPropagation）。
 */
export default function ProjectCard() {
  return null;
}