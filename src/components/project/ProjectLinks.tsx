/**
 * 卡片链接组（PRD F4.1）：按 kind 渲染图标按钮（仓库/Demo/文章/下载/安装命令）。M2 实现。
 * TODO(M2): 图标用 lucide-react（黑白线性，AGENTS §3）；
 * install 渲染命令文本 + CopyButton；外链 target="_blank" rel="noopener noreferrer"。
 */
export default function ProjectLinks() {
  return null;
}